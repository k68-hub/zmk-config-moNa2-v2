description: ZMK 20-degree rotation input processor

compatible: "zmk,input-processor-rotate-20"

properties:
  "#input-processor-cells":
    type: int
    const: 0
  
  track-remainders:
    type: boolean
    description: Track remainders for smoother input
